const SiteTheme = {
  green: "#00BD70"
};
const defaultDog = {
  label: "Husky",
  value: ["husky"]
};

const heroImage = require("../img/dogbg.jpg");
const defaultImage = require("../img/defaultdog.jpg");

export { SiteTheme, heroImage, defaultImage, defaultDog };
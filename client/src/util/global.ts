const SiteTheme = {
  green: "#00BD70"
};
const defaultDog = {
  label: "Husky",
  value: ["husky"]
};

const heroImage = require("../img/dogbg.jpg");
const defaultImage = require("../img/defaultdog.jpg");
const title = ["1rem", "1.5rem", "2rem", "3rem", "4rem"];
const header = ["0.8rem", "1rem", "1rem", "2rem", "3rem"];
const subheader = ["0.6rem", "0.8rem", "0.8rem", "1.5rem", "2rem"];

export { SiteTheme, heroImage, defaultImage, defaultDog, title, header, subheader };
